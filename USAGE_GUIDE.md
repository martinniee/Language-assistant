# 📖 Language Assistant 使用指南

## 🚀 安装步骤

### 方法一：手动安装

1. 将 `Language-assistant` 文件夹复制到您的 Obsidian vault 的以下路径：

    ```
    您的vault名称/.obsidian/plugins/Language-assistant/
    ```

2. 重启 Obsidian 或按 `Ctrl+R` 重新加载

3. 打开 Obsidian 设置 (⚙️)
    - 进入 "社区插件" 页面
    - 找到 "Language Assistant"
    - 点击开关启用插件

### 方法二：开发者模式

如果您想直接在当前开发环境测试：

1. 确保当前目录就在您的 vault 的 `.obsidian/plugins/` 下
2. 运行构建命令：`npm run build-app`
3. 在 Obsidian 中启用插件

## 📁 数据存储详解

### 存储位置

-   **文件**: `words.md`
-   **完整路径**: `vault根目录/words.md`
-   **示例**: `我的笔记本/words.md`

### 数据格式

插件使用标准 Markdown 格式存储数据，结构如下：

\`\`\`markdown

# 单词词汇表

## 单词名称

-   发音: /音标/
-   词汇: 单词本身
-   分类: 分类名称
-   标签: 标签 1,标签 2,标签 3
-   等级: 初级/中级/高级
-   查询次数: 数字
-   词性: 词性概述
-   内容: - 词性 1 (如：名词) - 定义 1 - 例句 1 - 例句 2 - 定义 2 - 例句 3 - 词性 2 (如：动词) - 定义 3 - 例句 4
    \`\`\`

## 🎮 使用方法

### 1. 打开插件界面

-   **方法一**: 点击左侧工具栏的书籍图标 📖
-   **方法二**: 按 `Ctrl+P` 打开命令面板，搜索 "打开单词管理页面"

### 2. 添加新单词

1. 点击 **"添加新单词"** 按钮
2. 填写基本信息：

    - **单词名称**: 必填，如 "hello"
    - **发音**: 音标，如 "/həˈloʊ/"
    - **分类**: 如 "日常用语"、"学术词汇"
    - **标签**: 用逗号分隔，如 "基础,常用,问候"
    - **等级**: 选择 初级/中级/高级
    - **词性概述**: 简述词性，如 "名词、动词"

3. 添加详细内容：

    - 点击 **"添加词性"** 创建词性分组
    - 为每个词性点击 **"添加定义"**
    - 为每个定义点击 **"添加例句"**

4. 点击 **"添加"** 保存

### 3. 编辑现有单词

1. 在单词列表中找到目标单词
2. 点击 **"编辑"** 按钮
3. 修改任何字段
4. 点击 **"更新"** 保存

### 4. 删除单词

1. 在单词列表中找到目标单词
2. 点击 **"删除"** 按钮
3. 单词将从界面和 `words.md` 文件中移除

## 📊 数据同步机制

### 自动同步

-   **UI → 文件**: 所有界面操作自动保存到 `words.md`
-   **文件 → UI**: 打开插件时自动加载最新数据
-   **实时更新**: 添加、编辑、删除操作立即生效

### 手动编辑

您也可以直接编辑 `words.md` 文件：

1. 在 Obsidian 中打开 `words.md`
2. 按照格式手动添加/修改内容
3. 重新打开插件界面会自动加载新内容

## 🔍 示例演示

### 当前示例数据

插件包含两个示例单词：

-   **hello**: 感叹词、名词，包含问候相关定义
-   **world**: 名词，包含"世界"和"领域"两个定义

### 添加新单词示例

假设要添加单词 "study"：

1. **基本信息**:

    - 名称: study
    - 发音: /ˈstʌdi/
    - 分类: 学习相关
    - 标签: 基础,学习,动词
    - 等级: 初级

2. **词性和定义**:
    - 动词
        - 学习；研究
            - I study English every day.
            - She studies medicine at university.
        - 仔细观察
            - He studied the map carefully.
    - 名词
        - 书房
            - My father works in his study.
        - 研究
            - This is an important study.

## 🛠️ 故障排除

### 常见问题

1. **插件无法启动**

    - 检查是否正确复制到 `.obsidian/plugins/` 目录
    - 确保 `main.js` 和 `manifest.json` 文件存在

2. **数据丢失**

    - 检查 vault 根目录是否有 `words.md` 文件
    - 查看 Obsidian 开发者控制台 (F12) 的错误信息

3. **界面显示异常**
    - 重启插件或重新加载 Obsidian
    - 检查 `words.md` 文件格式是否正确

### 数据备份

建议定期备份 `words.md` 文件，或使用 Git 版本控制。

---

**提示**: 这个插件完全兼容 Obsidian 的链接、标签等功能。您可以在 `words.md` 中使用 `[[链接]]` 和 `#标签` 等 Obsidian 语法！
